#include<stdio.h>

int main()
{
	int a[3]={1,2,3}, b, c;
	int *p, *p1;
	p1 =p = a;
	b = *p++;//因为 * 和++ 优先级相同 所以 *p++等价于*(p++） 但由于 p++是
				//先参与运算后地址指向下一位 所以*(p++）是先读取p然后赋值给b最后才地址指向下一位
	c = *++p1;
	printf("%d,%d",b, c);
	}